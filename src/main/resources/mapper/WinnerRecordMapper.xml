<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.lotterysystem.dao.mapper.WinnerRecordMapper">
    <insert id="batchInsert" useGeneratedKeys="true" keyProperty="id" keyColumn="id">
        insert into winning_record (
        activity_id, activity_name,
        prize_id, prize_name, prize_tier,
        winner_id, winner_name, winner_email, winning_time)
        values
        <foreach collection="items" item="item" index="index" separator=",">
            (#{item.activityId}, #{item.activityName},
            #{item.prizeId}, #{item.prizeName}, #{item.prizeTier},
            #{item.winnerId}, #{item.winnerName}, #{item.winnerEmail}, #{item.winningTime})
        </foreach>
    </insert>

    <delete id="deleteRecords">
        delete from winning_record
        where activity_id = #{activityId}
        <if test="prizeId != null" >
            and prize_id = #{prizeId}
        </if>
    </delete>

    <select id="selectByActivityId" resultType="com.example.lotterysystem.dao.dataobject.WinningRecordDO">
        select * from winning_record where activity_id = #{activityId}
    </select>

    <select id="countByAPId" resultType="java.lang.Integer">
        select count(1) from winning_record where activity_id = #{activityId} and prize_id = #{prizeId}
    </select>

    <select id="selectByActivityIdOrPrizeId" resultType="com.example.lotterysystem.dao.dataobject.WinningRecordDO">
        select * from winning_record
        where activity_id = #{activityId}
        <if test="prizeId != null">
            and prize_id = #{prizeId}
        </if>
    </select>
</mapper>
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.lotterysystem.dao.mapper.UserMapper">
    <insert id="insert" useGeneratedKeys="true" keyProperty="id" keyColumn="id">
        insert into user (user_name, email, phone_number, password, identity)
        values (#{userName}, #{email}, #{phoneNumber}, #{password}, #{identity})
    </insert>

    <select id="countByMail" resultType="int">
        select count(*) from user where email = #{email}
    </select>

    <select id="countByPhone" resultType="int">
        select count(*) from user where phone_number = #{phoneNumber}
    </select>

    <select id="selectByMail" resultType="com.example.lotterysystem.dao.dataobject.UserDO">
        select * from user where email = #{email}
    </select>

    <select id="selectByPhoneNumber" resultType="com.example.lotterysystem.dao.dataobject.UserDO">
        select * from user where phone_number = #{phoneNumber}
    </select>

    <select id="selectUserListByIdentity" resultType="com.example.lotterysystem.dao.dataobject.UserDO">
        select * from user
        <if test="identity!=null">
            where identity = #{identity}
        </if>
        order by id desc
    </select>
</mapper>
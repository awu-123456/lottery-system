<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.lotterysystem.dao.mapper.ActivityUserMapper">
    <insert id="batchInsert" useGeneratedKeys="true" keyProperty="id" keyColumn="id">
        insert into activity_user (activity_id, user_id, user_name, status)
        values
        <foreach collection="items" item="item" index="index" separator=",">
            (#{item.activityId}, #{item.userId}, #{item.userName}, #{item.status})
        </foreach>
    </insert>

    <update id="batchUpdateStatus">
        update activity_user set status = #{status}
        where activity_id = #{activityId}
        and user_id in
        <foreach collection="userIds" item="userId" open="(" separator="," close=")">
            #{userId}
        </foreach>
    </update>

    <select id="selectByActivityId" resultType="com.example.lotterysystem.dao.dataobject.ActivityUserDO">
        select * from activity_user where activity_id = #{activityId}
    </select>

    <select id="batchSelectByAUId" resultType="com.example.lotterysystem.dao.dataobject.ActivityUserDO">
        select * from activity_user
        where activity_id = #{activityId}
        and user_id in
        <foreach collection="userIds" item="userId" open="(" separator="," close=")">
            #{userId}
        </foreach>
    </select>
</mapper>
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.lotterysystem.dao.mapper.ActivityPrizeMapper">
    <insert id="batchInsert" useGeneratedKeys="true" keyProperty="id" keyColumn="id">
        insert into activity_prize (activity_id, prize_id, prize_amount, prize_tiers, status)
        values
        <foreach collection="items" item="item" index="index" separator=",">
            (#{item.activityId}, #{item.prizeId}, #{item.prizeAmount}, #{item.prizeTiers}, #{item.status})
        </foreach>
    </insert>

    <update id="updateStatus">
        update activity_prize set status = #{status} where activity_id = #{activityId} and prize_id = #{prizeId}
    </update>

    <select id="selectByActivityId" resultType="com.example.lotterysystem.dao.dataobject.ActivityPrizeDO">
        select * from activity_prize where activity_id = #{activityId}
    </select>

    <select id="selectByAPId" resultType="com.example.lotterysystem.dao.dataobject.ActivityPrizeDO">
        select * from activity_prize where activity_id = #{activityId} and prize_id = #{prizeId}
    </select>

    <select id="countPrize" resultType="java.lang.Integer">
        select count(1) from activity_prize where activity_id = #{activityId} and status = #{status}
    </select>
</mapper>